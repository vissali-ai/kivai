"use client";

import { openFilePicker } from "@/lib/browser/file-picker";
import { ChangeEvent, useRef, useState } from "react";
import { Copy, Download, Upload } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { ImageToolPageShell } from "@/components/tools/image-tool-page-shell";
import { canvasBlob, downloadBlob, IMAGE_TYPES, loadImage } from "@/lib/image-tools/canvas";

export function PlaceholderClient(){const input=useRef<HTMLInputElement>(null);const [file,setFile]=useState<File|null>(null);const [width,setWidth]=useState(32);const [quality,setQuality]=useState(55);const [result,setResult]=useState<{blob:Blob;url:string;data:string}|null>(null);const [copied,setCopied]=useState(false);const select=(e:ChangeEvent<HTMLInputElement>)=>{const next=e.target.files?.[0];if(!next||!IMAGE_TYPES.includes(next.type))return;setFile(next);setResult(null)};const generate=async()=>{if(!file)return;const image=await loadImage(file);const canvas=document.createElement("canvas");canvas.width=width;canvas.height=Math.max(1,Math.round(width*image.naturalHeight/image.naturalWidth));const ctx=canvas.getContext("2d");if(!ctx)return;ctx.imageSmoothingEnabled=true;ctx.imageSmoothingQuality="high";ctx.drawImage(image,0,0,canvas.width,canvas.height);const blob=await canvasBlob(canvas,"image/jpeg",quality/100);const data=await new Promise<string>((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(String(reader.result));reader.onerror=reject;reader.readAsDataURL(blob)});if(result)URL.revokeObjectURL(result.url);setResult({blob,url:URL.createObjectURL(blob),data})};return <ImageToolPageShell title="Gerador de Placeholder (LQIP)" description="Crie imagens leves de baixa qualidade para acelerar o carregamento visual das suas páginas."><Card className="mx-auto max-w-5xl"><CardHeader><CardTitle>Área de geração</CardTitle><CardDescription>Gere um JPEG leve e o respectivo Data URL para usar no seu projeto.</CardDescription></CardHeader><CardContent><input ref={input} type="file" accept="image/png,image/jpeg,image/webp" className="hidden" onChange={select}/>{!file?<div className="flex min-h-80 items-center justify-center border border-dashed border-border bg-muted/20"><Button onClick={()=>openFilePicker(input.current)}><Upload/>Selecionar imagem</Button></div>:<div className="grid gap-5 lg:grid-cols-2"><div className="border border-border bg-muted/20 p-4"><p className="font-medium">{file.name}</p><label className="mt-5 block text-sm font-medium">Largura: {width}px<input type="range" min="8" max="128" value={width} onChange={(e)=>setWidth(Number(e.target.value))} className="mt-2 w-full"/></label><label className="mt-4 block text-sm font-medium">Qualidade: {quality}%<input type="range" min="10" max="90" value={quality} onChange={(e)=>setQuality(Number(e.target.value))} className="mt-2 w-full"/></label><Button className="mt-6" onClick={generate}>Gerar placeholder</Button></div>{result?<div className="border border-border bg-background p-4"><img src={result.url} alt="Placeholder gerado" className="mx-auto max-h-64 max-w-full object-contain"/><p className="mt-4 text-sm text-muted-foreground">{(result.blob.size/1024).toFixed(1)} KB</p><div className="mt-4 flex flex-wrap gap-3"><Button onClick={()=>downloadBlob(result.blob,"placeholder-lqip.jpg")}><Download/>Baixar JPEG</Button><Button variant="outline" onClick={async()=>{await navigator.clipboard.writeText(result.data);setCopied(true)}}><Copy/>{copied?"Copiado":"Copiar Data URL"}</Button></div></div>:<div className="flex items-center justify-center border border-border bg-background p-4 text-sm text-muted-foreground">Configure e gere seu placeholder.</div>}</div>}</CardContent></Card></ImageToolPageShell>}
